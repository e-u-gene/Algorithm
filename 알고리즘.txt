2022/11/30

Scanner 통해서 콘솔창에 입력되는 값 받음
nextInt() -> int 형으로 입력되는 값을 받음. 넥스트 토큰 어쩌구
순차구조(sequential) -> 여러문장이 순차적으로 실행되는 구조. 위-> 아래

int max = a;
if(b > max) max = b;
if(c > max) max = c;
-> 얘처럼 () 안에 있는 식을 평가한 결과에 따라 프로그램의 실행흐름 변경 
-> 선택구조 ex)if 문

next() -> String으로 받음.